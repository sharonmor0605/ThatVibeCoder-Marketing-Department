---
name: connect-a-tool
description: Connect a new tool or service so claude can use it, the proper way: pick the right kind of connection, get a key on the free plan if one's needed, store it safely, and prove it works with one real call before moving on. Use when the owner wants to wire up an API, connect a new service, give claude access to something, hook claude up to a tool like Firecrawl or Stripe, or asks how to connect an account.
---

# connect-a-tool

This is the department's one proper way to wire any new tool in, key or no key.

Read `departments/it-systems/dev-team/api-manager/skills/wire-a-tool/SKILL.md` in full and follow it exactly. It opens by choosing the right kind of connection through `departments/it-systems/dev-team/api-manager/skills/pick-the-door/SKILL.md`, then gets the key, stores it, and proves it works. Do not improvise the sequence here; those files own it.
